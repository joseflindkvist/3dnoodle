import { createTheme } from "@mui/material";

export const themeMain = createTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#00ff00",
    },
  },
  typography: {
    fontFamily: "'Dosis Medium', sans-serif",
  },
});
